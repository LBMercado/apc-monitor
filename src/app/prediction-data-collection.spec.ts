import { PredictionDataCollection } from './prediction-data-collection';

describe('PredictionDataCollection', () => {
  it('should create an instance', () => {
    expect(new PredictionDataCollection()).toBeTruthy();
  });
});
