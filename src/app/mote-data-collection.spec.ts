import { MoteDataCollection } from './mote-data-collection';

describe('MoteDataCollection', () => {
  it('should create an instance', () => {
    expect(new MoteDataCollection()).toBeTruthy();
  });
});
